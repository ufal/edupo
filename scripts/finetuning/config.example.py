# Copy this file to config.py and fill in the paths for your environment.
# config.py is gitignored.

# Directory prefix for training output checkpoints (run name will be appended)
OUTPUT_PREFIX = "/path/to/outputs/"

# Path to the SQLite database
DB_PATH = "/path/to/new.db"

# Directory for training data logs (used with --debug)
LOG_PATH = "/path/to/logs/"
